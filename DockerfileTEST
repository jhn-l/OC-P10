# 🔹 Étape 1 : Image de construction
FROM python:3.12 AS build

ARG FUNCTION_DIR="/function"
RUN mkdir -p ${FUNCTION_DIR}
WORKDIR ${FUNCTION_DIR}

# ✅ Installer les outils de compilation nécessaires
RUN apt-get update && apt-get install -y \
    gcc g++ make libstdc++-12-dev libgomp1 \
    && rm -rf /var/lib/apt/lists/*

# ✅ Forcer NumPy à rester en version 1.x pour éviter les erreurs de compatibilité
RUN pip install --no-cache-dir numpy<2

# ✅ Installer les dépendances
RUN pip install --no-cache-dir --target ${FUNCTION_DIR} \
    scikit-learn surprise boto3 pandas

# 🔹 Étape 2 : Entraînement du modèle
FROM build AS train
WORKDIR /function

# ✅ Définir le chemin du modèle
ENV MODEL_PATH=/tmp/recommender_model_hybrid.pkl
ENV SURPRISE_DATASET_DIR="/tmp"

# ✅ Copier le script d'entraînement
COPY train.py .

# ✅ Exécuter l'entraînement du modèle
RUN python3 train.py

# 🔹 Étape 3 : Image finale pour AWS Lambda
FROM python:3.12-slim AS final
ARG FUNCTION_DIR="/function"
WORKDIR ${FUNCTION_DIR}

# ✅ Copier uniquement les dépendances préinstallées
COPY --from=build ${FUNCTION_DIR} ${FUNCTION_DIR}

# ✅ Copier le modèle entraîné depuis l’étape `train`
COPY --from=train /tmp/recommender_model_hybrid.pkl ${FUNCTION_DIR}/recommender_model_hybrid.pkl

# ✅ Copier le fichier de recommandation (Lambda handler)
COPY hybrid_recommender.py .

# ✅ Définir le Runtime Interface Client pour AWS Lambda
ENTRYPOINT [ "/usr/local/bin/python", "-m", "awslambdaric" ]
CMD ["hybrid_recommender.lambda_handler"]
