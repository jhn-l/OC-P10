# Utilisation de l'image AWS Lambda Python 3.8
FROM public.ecr.aws/lambda/python:3.8

# Définition du répertoire de travail pour AWS Lambda
WORKDIR /var/task

# 🔹 Installer les outils de compilation nécessaires pour `scikit-surprise`
RUN yum install -y gcc gcc-c++ make libstdc++-devel

# 🔹 Définition des variables d'environnement pour éviter les erreurs de Surprise
ENV SURPRISE_DATASET_DIR=/tmp

# 🔹 Installation des dépendances Python dans `/var/task` pour AWS Lambda
RUN pip install --no-cache-dir pandas numpy scikit-learn scikit-surprise boto3 \
    --target /var/task

# 🔹 Copier le fichier source dans le conteneur
COPY hybrid_recommender.py .

# 🔹 Définition du point d'entrée de la Lambda
CMD ["hybrid_recommender.lambda_handler"]
