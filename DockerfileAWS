# 🔹 Étape 1 : Base AWS Lambda
FROM public.ecr.aws/lambda/python:3.8 as base

# 🔹 Étape 2 : Entraînement du modèle
FROM base AS train
WORKDIR /var/task

RUN yum install -y gcc gcc-c++ make libstdc++-devel

# Installer les dépendances pour l'entraînement
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Définir le chemin du modèle pour être cohérent avec `train.py`
ENV MODEL_PATH=/tmp/recommender_model_hybrid.pkl

# Copier le script d'entraînement et l’exécuter
COPY train.py .
RUN python3 train.py

# 🔹 Étape 3 : Construction de l’image finale pour AWS Lambda
FROM base
WORKDIR /var/task

# ✅ **Installer à nouveau `gcc` avant `pip install`**
RUN yum install -y gcc gcc-c++ make libstdc++-devel

# Installer `scikit-learn`, `surprise` et `boto3` pour l’inférence
RUN pip install --no-cache-dir scikit-learn sickit-surprise boto3

# Copier le modèle entraîné depuis l’étape `train`
COPY --from=train /tmp/recommender_model_hybrid.pkl /var/task/recommender_model_hybrid.pkl

# Copier le fichier de recommandation (Lambda handler)
COPY hybrid_recommender.py .

# 🔹 Définition du point d’entrée de la Lambda
CMD ["hybrid_recommender.lambda_handler"]
